#include "stdafx.h"

int sk_zip_compress(const char *src, size_t nsrc, char **dest, size_t *ndest) {
  return SK_NO;
}
int sk_zip_uncompress(const char *src, size_t nsrc, char **dest,
                      size_t *ndest) {
  return SK_NO;
}
int sk_gzip_compress(const char *src, size_t nsrc, char **dest, size_t *ndest) {
  return SK_NO;
}
int sk_gzip_uncompress(const char *src, size_t nsrc, char **dest,
                       size_t *ndest) {
  return SK_NO;
}
int sk_zstd_compress(const char *src, size_t nsrc, char **dest, size_t *ndest) {
  return SK_NO;
}
int sk_zstd_uncompress(const char *src, size_t nsrc, char **dest,
                       size_t *ndest) {
  return SK_NO;
}